#include <stdio.h>
/**
*main - prints all single digit numbers of base ten from zero
*Description: using the main function
*this program prints all single digit numbers of base ten from zero
*Return: 0
*/
int main(void)
{
int i;
for (i = 0 ; i < 10 ; i++)
{
	printf("%i", i);
}
printif("\n");
return (0);
}
